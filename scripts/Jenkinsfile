pipeline {
    agent { docker { image pipeline {     agent { docker { im 'python:3.5.1' } }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}
